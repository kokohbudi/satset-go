<?xml version="1.0" encoding="UTF-8"?>
<Configuration status="WARN">
    <Properties>
        <Property name="LOG_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} %highlight{%-5level}{FATAL=red blink, ERROR=red, WARN=yellow bold, INFO=green, DEBUG=blue, TRACE=white} %style{[%t]}{magenta} %style{%c{1.}}{cyan} : %msg%n
        </Property>
        <Property name="FILE_PATTERN">
            %d{yyyy-MM-dd HH:mm:ss.SSS} [%t] %-5level %c{1.} : %msg%n
        </Property>
    </Properties>

    <Appenders>
        <Console name="Console" target="SYSTEM_OUT">
            <PatternLayout pattern="${LOG_PATTERN}"/>
        </Console>

        <RollingFile name="File" fileName="logs/omnip-services.log"
                     filePattern="logs/omnip-services-%d{yyyy-MM-dd}-%i.log.gz">
            <PatternLayout pattern="${FILE_PATTERN}"/>
            <Policies>
                <TimeBasedTriggeringPolicy/>
                <SizeBasedTriggeringPolicy size="10MB"/>
            </Policies>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>
    </Appenders>

    <Loggers>
        <!-- AsyncLogger untuk package aplikasi sendiri (DEBUG level) -->
        <AsyncLogger name="com.satset" level="debug" additivity="false">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </AsyncLogger>

        <!-- AsyncLogger untuk Spring Security -->
        <AsyncLogger name="org.springframework.security" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </AsyncLogger>

        <!-- AsyncLogger untuk Hibernate SQL -->
        <AsyncLogger name="org.hibernate.SQL" level="info" additivity="false">
            <AppenderRef ref="Console"/>
        </AsyncLogger>

        <!-- AsyncRoot: semua logger lain -->
        <AsyncRoot level="info">
            <AppenderRef ref="Console"/>
            <AppenderRef ref="File"/>
        </AsyncRoot>
    </Loggers>
</Configuration>
