app = "satset-wg-gateway"
primary_region = "sin"

[build]

[[services]]
  internal_port = 8888
  protocol = "tcp"

  [[services.ports]]
    port = 8888
    # handlers kosong = raw TCP passthrough (proxy CONNECT butuh ini, jangan TLS-terminate)
